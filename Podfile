# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleiOSFramework' do
  pod 'Alamofire', '~> 4.7'
  target 'SampleiOSFrameworkTests' do
  end
end

target 'SampleMacFramework' do
  pod 'Alamofire', '~> 4.7'
  target 'SampleMacFrameworkTests' do
  end

end
